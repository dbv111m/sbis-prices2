# Инсайты по скрапингу тарифов saby.ru

Документ фиксирует все findings по устройству сайта, ценообразованию и
оптимизации сбора данных. Собрано в ходе полного скрапинга 22.09.2026
(20 вкладок × 90 регионов = 36 090 записей, 254 услуги).

---

## 1. Как сайт отдаёт цены (ключевая находка)

- **Цены вшиты в серверный HTML.** Страница `https://saby.ru/tariffs?region={id}&tab={tab}`
  по обычному HTTP-запросу (без браузера, без JS) уже содержит все кнопки цен
  с атрибутами `nomenclaturecode`. Проверено сверкой: HTTP vs Playwright —
  совпадение цен 37/37 байт-в-байт.
- **Следствие:** Playwright не обязателен. HTTP-скрапер (`http_scraper.py`) на
  чистом stdlib делает регион за ~1.1 сек вместо ~10 сек в браузере (**~10x**).
- **Бот-детект по User-Agent.** С устаревшими UA (Chrome ≤109 из примеров
  2022 года) сайт редиректит на страницу «Диагностика рабочего места»
  («Обновите браузер»). С современными UA (Chrome 138–140) всё работает.
  Симптом: `wait_for_selector` падает по таймауту, в логе навигация на
  `/cdn/Maintenance/.../diagnostic.html`.
- **JSON-RPC `/service/?x_version=...`** на странице отвечает сервисами
  аккаунта, а не тарифами — как источник цен не подходит.
- **Навигация по вкладкам — JS**, в сыром HTML ссылок `tab=` нет. Полный
  список из 20 значений `tab=` получен программным кликом по вкладкам
  (см. `TABS` в `http_scraper.py` / `optimized_scraper.py`).

## 2. Структура данных цен

- Кнопка цены: `span.billing-PriceList__priceButton[nomenclaturecode="..."]`,
  текст кнопки = цена («3 000», неразрывные пробелы).
- **Одна строка сайта = несколько кодов** (пакеты: базовый/опт/профи, объёмы
  документов, варианты для ИП/ЮЛ). Смысловой уровень пакета зашит в суффикс
  кода (`_opt`, `_prof`, `EDO_500_D`, `ETRN_10000`, `EOpSBISfrmLIoN`...),
  поэтому расшифровка ведётся через `nomenclature_map.json`.
- **Разметка заголовков услуг неоднородна** между блоками страницы:
  таблицы (`table-title-header` в первой ячейке `<tr>`), карточки
  (`MobileService-header`), планы (`title-sub`, `multi-serviceTitle`).
  HTTP-парсер берёт **ближайший предшествующий кнопке заголовок** — это даёт
  100% покрытие имён почти на всех вкладках (85–95% на desk/med/meet из-за
  одиночных кнопок без заголовка).
- **Номенклатура сайта живая:** между октябрём 2025 и сентябрём 2026
  появились `EDI_horeca`, `Ent_ausn`, тарифы МВД разбились на
  `EOMVD_staff10/50/100/500` вместо одного `EO_MVD`. Гибридная схема
  (имена с сайта + mapping) переваривает это без правок кода.

## 3. Ценообразование (по 254 услугам, 22.09.2026)

- **Региональности почти нет:** 16 из 20 вкладок имеют единую цену по всей
  стране (1 кластер = все 90 регионов идентичны). Из 254 услуг лишь ~22
  зависят от региона.
- **Вся региональность сосредоточена в планах отчётности** (`EOpSBISfrm*`,
  вкладка ereport): 12 услуг с 9–13 ценовыми уровнями каждая, разброс до 4–5
  раз (пример: «Корпоративный» UP050N — от 29 000 ₽ в Чувашии до 140 000 ₽
  в Москве).
- **Устойчивые ценовые зоны** (консистентны между планами):
  - Чувашия — почти всегда минимум;
  - Архангельская, Вологодская, Астраханская — дешёвая зона;
  - Кавказские республики и юг — средняя зона;
  - Бурятия, Карелия, Татарстан, Коми, СПб — выше средней;
  - Новосибирская, Пермский край, Самарская, Иркутская — дорогая зона;
  - **Москва + Московская обл. — всегда максимум** (идут парой во всех услугах
    с наценкой 11–250% к стране: Центробанк 3 500 против 1 000, АУСН 5 000
    против 3 000);
  - ХМАО — отдельная дорогая зона в «Корпоративном» (105 000 ₽);
  - Новые регионы (ДНР, Луганская, Запорожская, Херсонская) — в дешёвой зоне.
- **Локальные исключения:** «ЭДО — Базовый» — 3 зоны (86 регионов по 2 300 ₽,
  Самарская обл.+Тольятти 2 500 ₽, Москва+МО 3 000 ₽); одна услуга на вкладке
  «Торговля и производство» также зонная.

## 4. Кластеры регионов и оптимизация сбора

Кластер = группа регионов с идентичными ценами на **все** услуги вкладки
(см. `region_clusters.json`, обновляется `analyze_prices.py --save-clusters`):

| Вкладка | Кластеров | Практика |
|---|---|---|
| 16 вкладок (кроме трёх ниже) | 1 | достаточно 1 региона |
| ЭДО, Торговля и производство | 3 | достаточно 3 регионов |
| Отчетность и бухгалтерия | 76 | регионы почти уникальны |

- **Режим мониторинга цен:** 17 вкладок × 1 регион + полный проход только
  ereport ≈ **5 минут** на всю картину (вместо ~45 мин полного прогона).
- Live-проверка устойчивости: члены кластеров дают идентичные векторы при
  повторном скрапе; граница кластеров (Москва vs СПб) различается ровно
  ожидаемой услугой.
- Кластеры пересчитываются после каждого полного прогона — если Saby
  перетасует зоны, `analyze_prices.py` это покажет.

## 5. Производительность

| Способ | На регион | Вкладка (90 рег.) | 20 вкладок |
|---|---|---|---|
| Playwright (`optimized_scraper.py`) | ~10 сек | ~12 мин | ~6 часов |
| HTTP (`http_scraper.py`) | ~1.1 сек | ~2 мин | **~45 мин** |
| HTTP + кластеры (17×1 + ereport) | — | — | **~5 мин** |

## 6. Надёжность (если что-то заблокируют)

1. **HTTP-скрапер** — основной путь. Retry на регион (по умолчанию 3),
   нарастающие паузы. Пустой ответ / страница диагностики трактуется как
   ошибка попытки.
2. **Playwright-скрапер** — полный fallback: реальный браузер, случайные
   UA/viewport, случайные паузы, сбор имён услуг с сайта. Формат вывода
   идентичен HTTP-скраперу — остальные скрипты не замечают подмены.
3. **Прокси** — заготовка в `optimized_scraper.py` (раскомментировать
   `proxy={"server": ...}` в `parse_nomenclature_prices`).
4. Оба скрапера поддерживают `--limit` / `--regions` для точечных проверок
   без полного прогона.

## 7. Человекочитаемые отчёты

- Приоритет названия: **mapping → имя с сайта → код**; незнакомый код →
  группа «Прочее» + предупреждение в консоли (дополнить `nomenclature_map.json`).
- 254/254 кодов расшифрованы: ~75 вручную (с проверкой DOM), 176
  автоматически (имя с сайта + группа = категория вкладки, помечены
  `"_auto": true` — кандидаты на ручное уточнение), 3 вручную по смыслу кода.
- Суффиксы планов отчётности (`LIoN`, `B2o`, `UP005N`...) — варианты тарифа
  по типу клиента (ИП/ЮЛ, с сотрудниками/без); точная расшифровка есть
  только у Saby, поэтому суффикс сохранён в названии.
- CSV — плоский (`Группа | Услуга | Код | регионы`), Excel — секции по
  группам с закреплённой шапкой (удобно листать 90 регионов).

## 8. Известные ограничения и оговорки

- HTTP-скрапер зависит от вёрстки сайта: классы заголовков
  (`table-title-header`, `title-sub`, ...) могут измениться — при падении
  покрытия имён смотреть парсер `http_scraper.py`.
- Числа после программного «переклика» вкладок в браузере завышены
  (DOM в переходном состоянии содержит кнопки соседних вкладок) —
  источником истины считать серверный HTML.
- `EOpEDOvn_D` назван «Отчетность + внешний ЭДО (пакет)» по позиции в
  ценовом ряду — кандидат на уточнение.
